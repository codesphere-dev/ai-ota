[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "ai-ota"
version = "0.1.0"
description = "Python SDK to build and package AI models into OTA updates"
requires-python = ">=3.10"
license = { text = "GPL-3.0-only" }

[tool.maturin]
python-source = "."

[tool.ruff]
line-length = 100